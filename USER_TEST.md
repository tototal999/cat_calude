# ClaudeCat 人工驗收清單

> 目的：在交付給同事前，由測試人員以新版 `dist\ClaudeCat\ClaudeCat.exe` 實際操作確認。
>
> 原則：不要在截圖、回報或 Git 中貼入內網 API URL、API Key、Token、客戶資料或機密文件全文。

---

## 0. 測試資訊

| 欄位 | 填寫 |
|---|---|
| 測試日期 |  |
| 測試人員 |  |
| EXE 版本／檔案時間 |  |
| Release manifest／EXE SHA-256 是否相符 | 是／否 |
| Windows 版本 |  |
| 公司 LLM 首次啟動是否自動套用 | 是／否 |
| 是否安裝並登入 Claude Desktop | 是／否 |
| 是否安裝並登入 Codex Desktop | 是／否 |

### 執行前準備

1. 保留完整 `dist\ClaudeCat` 資料夾，從其中的 `ClaudeCat.exe` 啟動程式；不可只複製 EXE。
2. 準備一份無敏感資料的文字型 PDF、DOCX、PPTX、XLSX 測試檔；掃描型 PDF 可另備一份。
3. 新使用者首次啟動時，公司發布設定應自動寫入 `%LOCALAPPDATA%\ClaudeCat\config.json`；**不可在回報中貼出其內容**。
4. 對照 `dist\ClaudeCat_release-manifest.json` 的 `executable_sha256` 與實際 EXE；正式發布前確認
   `verification` 含 `gui-smoke`，並由發布人員確認 `git_dirty` 狀態是否符合本次發布規範。

驗收結果標記：`o`＝通過、`x`＝不通過、`-`＝跳過（不具條件）。

### 目前進度（2026-07-22）

- 舊版結果可沿用：25／57。
- 本輪政策停用 JSON、進階模型設定、排程、文件會議包、比較文件與聊天附件；對應項目應確認
  「入口完全隱藏」後標示 `-（政策停用）`，不可因找不到入口判定為 bug。
- 新版待複驗以表格中仍開放且尚未填寫結果的項目為準。

---

## 1. 啟動與桌寵

| # | 操作 | 預期結果 | 結果／備註 |
|---|---|---|---|
| 1.1 | 啟動 EXE，等待 5 秒 | 桌面出現貓咪；程式不閃退、不出現命令列視窗 | o |
| 1.2 | 左鍵拖曳貓咪 | 貓咪可移動到新位置 | o |
| 1.3 | 關閉後重新啟動 | 貓咪位置保留| o |
| 1.4 | 單擊貓咪 | 貓旁出現「問我一句」小輸入框，不開完整視窗 | o |
| 1.5 | 在小輸入框按 `×` | 小輸入框關閉，貓咪仍正常顯示| o |

## 2. 右鍵選單與 Skin

| # | 操作 | 預期結果 | 結果／備註 |
|---|---|---|---|
| 2.1 | 右鍵點貓咪 | 看到快速提問、交談、文件助手與翻譯；目前政策下不顯示 JSON、模型設定與排程 |  |
| 2.2 | 檢查下半部選項 | 看到：切換 Skin、寵物設定、測試、開啟記錄、清除記錄、結束| o |
| 2.3 | 開啟「切換 Skin」 | 可看到可用 Skin；選擇任一 Skin 後貓咪立即變更 | o |
| 2.4 | 重新啟動程式 | 上一步選擇的 Skin 仍保留 | o |
| 2.5 | 開啟「寵物設定」 | 可調整置頂、用量顯示、Claude／Codex limits、朝向、尺寸與更新間隔 | o |

## 3. LLM 介面與快速提問

> 沒有公司 LLM 設定時，預期應顯示明確連線／設定錯誤；不可靜默失敗。

| # | 操作 | 預期結果 | 結果／備註 |
|---|---|---|---|
| 3.1 | 右鍵 →「交談（LLM 介面）…」 | 開啟完整交談視窗，Windows 工作列可切回；左側不顯示公司已關閉的進階模型設定 |  |
| 3.2 | 輸入「請只回答：測試成功」並送出 | 已設定 LLM 時取得正常回答；未設定時顯示可理解錯誤 | o |
| 3.3 | 單擊貓咪，輸入短問題並按 Enter | 問題在貼身泡泡送出；可收到回答或明確錯誤 | o |
| 3.4 | 關閉完整交談視窗 | 視窗隱藏／關閉後，貓咪恢復原本尺寸與位置 | o |
| 3.5 | 最大化完整交談視窗後再關閉 | 貓咪與用量徽章恢復原尺寸且完整留在螢幕內，不會被保存到畫面外 | o |
| 3.6 | 檢查頂端模型選單；若公司提供多個模型則切換後提問 | 只列公司核准模型；切換後回答使用所選模型並於重啟後保留 |  |

| 3.7 | 使用沒有 ClaudeCat 設定的新 Windows 帳號啟動公司發布包 | 不需附帶設定檔或手動輸入 URL／模型即可使用公司 LLM；不得顯示 localhost |  |

## 4. JSON 工具（目前政策停用）

目前發布版應完全隱藏 JSON 入口；4.2～4.7 保留供未來重新開放後複驗。

測試資料：

```json
{"model":"CC","messages":[{"role":"user","content":"你是什麼模組?"}]}
```

| # | 操作 | 預期結果 | 結果／備註 |
|---|---|---|---|
| 4.1 | 檢查右鍵選單與完整介面側欄 | 不顯示 JSON 工具入口 |  |
| 4.2～4.7 | JSON 格式化、壓縮、驗證、Tree View 與搜尋 | 本輪不執行 | -（政策停用） |

## 5. 翻譯與公司模型限制

> 此區需公司 LLM 可連線。若未配置，記錄明確錯誤後可標示跳過。

| # | 操作 | 預期結果 | 結果／備註 |
|---|---|---|---|
| 5.1 | 右鍵 →「翻譯…」 | 直接開啟翻譯頁  | o |
| 5.2 | 輸入 `Please approve the purchase order.`，按翻譯 | 取得繁中譯文  | o |
| 5.3 | 輸入含 `SELECT ORG_ID FROM PO_HEADERS;` 的文字，保留程式碼勾選 | SQL 內容不被翻譯或改寫 | o |
| 5.4 | 目標選擇「簡體中文」後翻譯繁中內容 | 取得簡體中文譯文，不混用繁體字形 | o |
| 5.5 | 按來源與目標中間的 `⇄` | 來源與目標互換；已有譯文時移回原文框，可直接反向翻譯 | o |
| 5.6 | 檢查右鍵選單與左側分頁 | 公司版不顯示可修改 Provider／API URL 的進階模型設定 |  |
| 5.7 | 在測試帳號關閉程式，把執行期 LLM URL／模型改成無效外部值後重啟 | 外部值被公司內嵌設定取代；聊天仍只使用公司端點與核准模型 |  |

## 6. 文件助手與引用

> 文件與索引只保留於本機；文件回答使用公司 LLM。請只使用非敏感測試文件。

| # | 操作 | 預期結果 | 結果／備註 |
|---|---|---|---|
| 6.1 | 右鍵 →「文件助手…」，選擇文字型 PDF | 顯示建立本機索引的進度，完成後可提問 |  |
| 6.2 | 問一個測試文件明確有寫的問題 | 回答附檔名與頁碼／段落／投影片／工作表等來源定位 |  |
| 6.3 | 問文件沒有提到的問題 | 顯示「此文件沒有描述此問題，無法依文件確認。」或同義明確拒答 |  |
| 6.4 | 分別選 DOCX、PPTX、XLSX 測試檔 | 可建立索引；來源分別可定位至章節／投影片／工作表與儲存格範圍 |  |
| 6.5 | 選擇掃描型 PDF | 若沒有可擷取文字，明確提示需要 OCR；不可假裝已讀懂 |  |
| 6.6 | 移除一筆文件索引 | 僅移除本機索引，不刪除原始文件 |  |
| 6.7 | 檢查文件助手操作區 | 不顯示文件會議包與比較文件入口 |  |
| 6.8～6.13 | 文件會議包、翻譯、取消、重試與歷史清理 | 本輪不執行 | -（政策停用） |

## 7. Claude／Codex 用量（選用）

> 這些開關只讀取本機登入狀態，**不影響聊天、翻譯或文件問答**。首次啟用必須由每位使用者在自己的電腦同意。

| # | 操作 | 預期結果 | 結果／備註 |
|---|---|---|---|
| 7.1 | 未安裝／未登入 Claude 或 Codex 的電腦啟用開關 | 顯示 `No use` 或明確原因；程式不當掉 |  |
| 7.2 | 已登入 Claude Desktop 時啟用 Claude limits | 徽章或選單顯示 Claude 用量；無法讀取時有明確原因 |  |
| 7.3 | 已登入 Codex Desktop 時啟用 Codex limits | 徽章或選單顯示 Codex 用量；需要登入時明確提示 |  |
| 7.4 | 同時啟用 Claude 與 Codex limits | 徽章分成上下兩行：第一行 Claude、第二行 Codex |  |
| 7.5 | 任一用量超過 90%（若可測） | 對應徽章變紅警示 |  |
| 7.6 | 關閉兩個 limits | 用量徽章完全隱藏、不顯示 `OFF`；聊天與文件助手仍可正常使用 |  |

## 8. 系統匣、排程與結束

| # | 操作 | 預期結果 | 結果／備註 |
|---|---|---|---|
| 8.1 | 在工作列右下角右鍵 ClaudeCat 圖示 | 看到顯示貓、隱藏貓、快速提問、文件助手、結束 |  |
| 8.2 | 選「隱藏貓」再選「顯示貓」 | 貓咪與用量徽章正確隱藏／顯示 |  |
| 8.3 | 檢查右鍵選單與完整介面側欄 | 不顯示排程入口 |  |
| 8.4 | 右鍵 →「開啟記錄」 | 顯示本機記錄；記錄中不應有 API Key 或 Token |  |
| 8.5 | 右鍵 →「清除記錄…」後選擇「否」 | 記錄保留，不被清除 |  |
| 8.6 | 再次選擇「清除記錄…」並確認 | 目前及輪替記錄被清除；程式繼續運作並可產生新記錄 |  |
| 8.7 | 系統匣或右鍵 →「結束」 | 貓咪、徽章與背景程序都結束；再次可正常啟動 |  |

---

## 問題回報格式

請每個問題附上：

1. 驗收編號（例如 `6.2`）。
2. 操作步驟與實際結果。
3. 截圖（先遮蔽文件內容、帳號、內網 URL 與憑證）。
4. `%LOCALAPPDATA%\ClaudeCat\claudecat.log` 的相關時間附近訊息（先遮蔽敏感內容）。

## 最終結論

- [ ] 可交付測試使用者
- [ ] 有阻塞問題，需修正後複驗

測試人員簽名／日期：____________________________
