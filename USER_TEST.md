# ClaudeCat 人工驗收清單

> 目的：在交付給同事前，由測試人員以新版 `dist\ClaudeCat\ClaudeCat.exe` 實際操作確認。
>
> 原則：不要在截圖、回報或 Git 中貼入內網 API URL、API Key、Token、客戶資料或機密文件全文。

---

## 0. 測試資訊

| 欄位 | 填寫 |
|---|---|
| 測試日期 |  |
| 測試人員 |  |
| EXE 版本／檔案時間 |  |
| Windows 版本 |  |
| 是否已設定公司 LLM | 是／否 |
| 是否安裝並登入 Claude Desktop | 是／否 |
| 是否安裝並登入 Codex Desktop | 是／否 |

### 執行前準備

1. 從 `dist\ClaudeCat\ClaudeCat.exe` 啟動程式。
2. 準備一份無敏感資料的文字型 PDF、DOCX、PPTX、XLSX 測試檔；掃描型 PDF 可另備一份。
3. 若測試 LLM，確認 `%LOCALAPPDATA%\ClaudeCat\config.json` 已由公司安裝程序配置；**不可在回報中貼出其內容**。

驗收結果標記：`[ ] 通過`、`[ ] 不通過`、`[ ] 跳過（不具條件）`

---

## 1. 啟動與桌寵

| # | 操作 | 預期結果 | 結果／備註 |
|---|---|---|---|
| 1.1 | 啟動 EXE，等待 5 秒 | 桌面出現貓咪；程式不閃退、不出現命令列視窗 |  |
| 1.2 | 左鍵拖曳貓咪 | 貓咪可移動到新位置 |  |
| 1.3 | 關閉後重新啟動 | 貓咪位置保留 |  |
| 1.4 | 單擊貓咪 | 貓旁出現「問我一句」小輸入框，不開完整視窗 |  |
| 1.5 | 在小輸入框按 `×` | 小輸入框關閉，貓咪仍正常顯示 |  |

## 2. 右鍵選單與 Skin

| # | 操作 | 預期結果 | 結果／備註 |
|---|---|---|---|
| 2.1 | 右鍵點貓咪 | 看到：快速提問、交談（LLM 介面）、文件助手、JSON 工具、翻譯、模型設定、排程 |  |
| 2.2 | 檢查下半部選項 | 看到：切換 Skin、寵物設定、測試、開啟記錄、結束 |  |
| 2.3 | 開啟「切換 Skin」 | 可看到可用 Skin；選擇任一 Skin 後貓咪立即變更 |  |
| 2.4 | 重新啟動程式 | 上一步選擇的 Skin 仍保留 |  |
| 2.5 | 開啟「寵物設定」 | 可調整置頂、用量顯示、Claude／Codex limits、朝向、尺寸與更新間隔 |  |

## 3. LLM 介面與快速提問

> 沒有公司 LLM 設定時，預期應顯示明確連線／設定錯誤；不可靜默失敗。

| # | 操作 | 預期結果 | 結果／備註 |
|---|---|---|---|
| 3.1 | 右鍵 →「交談（LLM 介面）…」 | 開啟完整交談視窗，左側有 Chat、文件助手、JSON 工具、翻譯、模型設定、Schedule |  |
| 3.2 | 輸入「請只回答：測試成功」並送出 | 已設定 LLM 時取得正常回答；未設定時顯示可理解錯誤 |  |
| 3.3 | 單擊貓咪，輸入短問題並按 Enter | 問題在貼身泡泡送出；可收到回答或明確錯誤 |  |
| 3.4 | 關閉完整交談視窗 | 視窗隱藏／關閉後，貓咪恢復原本尺寸與位置 |  |
| 3.5 | 最大化完整交談視窗後再關閉 | 貓咪與用量徽章恢復原尺寸且完整留在螢幕內，不會被保存到畫面外 |  |

## 4. JSON 工具（不需要 LLM）

測試資料：

```json
{"model":"CC","messages":[{"role":"user","content":"你是什麼模組?"}]}
```

| # | 操作 | 預期結果 | 結果／備註 |
|---|---|---|---|
| 4.1 | 右鍵 →「JSON 工具…」 | 完整視窗直接開啟 JSON 工具頁 |  |
| 4.2 | 貼上測試資料，按「格式化」 | 結果顯示正確縮排 JSON |  |
| 4.3 | 按「壓縮」 | 結果為單行 JSON，資料內容不變 |  |
| 4.4 | 按「找錯誤」 | 顯示 JSON 格式正確 |  |
| 4.5 | 將最後一個 `}` 刪除後按「找錯誤」 | 顯示錯誤行與欄，不當掉 |  |
| 4.6 | 還原合法 JSON，檢查 Tree View | 可展開 `messages`、`[0]`、`content`；點選節點顯示 JSONPath |  |
| 4.7 | 使用搜尋 `content` | 顯示對應節點／路徑 |  |

## 5. 翻譯與模型設定

> 此區需公司 LLM 可連線。若未配置，記錄明確錯誤後可標示跳過。

| # | 操作 | 預期結果 | 結果／備註 |
|---|---|---|---|
| 5.1 | 右鍵 →「翻譯…」 | 直接開啟翻譯頁 |  |
| 5.2 | 輸入 `Please approve the purchase order.`，按翻譯 | 取得繁中譯文 |  |
| 5.3 | 輸入含 `SELECT ORG_ID FROM PO_HEADERS;` 的文字，保留程式碼勾選 | SQL 內容不被翻譯或改寫 |  |
| 5.4 | 右鍵 →「模型設定…」 | 直接開啟模型設定頁，不顯示 API Key |  |
| 5.5 | 按「測試模型連線」 | 連線成功，或顯示可理解的失敗原因 |  |

## 6. 文件助手與引用

> 文件與索引只保留於本機；文件回答使用公司 LLM。請只使用非敏感測試文件。

| # | 操作 | 預期結果 | 結果／備註 |
|---|---|---|---|
| 6.1 | 右鍵 →「文件助手…」，選擇文字型 PDF | 顯示建立本機索引的進度，完成後可提問 |  |
| 6.2 | 問一個測試文件明確有寫的問題 | 回答附檔名與頁碼／段落／投影片／工作表等來源定位 |  |
| 6.3 | 問文件沒有提到的問題 | 顯示「此文件沒有描述此問題，無法依文件確認。」或同義明確拒答 |  |
| 6.4 | 分別選 DOCX、PPTX、XLSX 測試檔 | 可建立索引；來源分別可定位至章節／投影片／工作表與儲存格範圍 |  |
| 6.5 | 選擇掃描型 PDF | 若沒有可擷取文字，明確提示需要 OCR；不可假裝已讀懂 |  |
| 6.6 | 移除一筆文件索引 | 僅移除本機索引，不刪除原始文件 |  |
| 6.7 | 選擇 PDF／DOCX 後按「建立文件會議包」 | 三次操作內開始 Workflow；依序顯示檢索、摘要、會議重點與匯出狀態 |  |
| 6.8 | 等待文件會議包完成 | 顯示 coverage、來源檔名與頁碼／段落，以及完整 Markdown 成果路徑 |  |
| 6.9 | 開啟顯示的 Markdown 路徑 | 內容含摘要、會議重點、coverage 說明與來源；抽樣時不可宣稱完整結論 |  |
| 6.10 | 勾選「加入英文翻譯」後再建立一次 | Workflow 多一個翻譯步驟，原始中文摘要與會議重點仍保留 |  |
| 6.11 | 在 Workflow 執行時按「取消」 | 最遲於目前 LLM 請求完成後停止，不再執行下一步，狀態顯示 cancelled |  |
| 6.12 | 對失敗／取消 Run 按「重新執行」 | 建立新的 Run 並重新執行；完成 Run 不顯示重試，超過 3 次會明確拒絕 |  |
| 6.13 | 按「清理 Workflow 歷史」並確認 | 已結束 Run 與 Markdown 成果被清除；若有執行中工作則明確顯示保留數量 |  |

## 7. Claude／Codex 用量（選用）

> 這些開關只讀取本機登入狀態，**不影響聊天、翻譯或文件問答**。首次啟用必須由每位使用者在自己的電腦同意。

| # | 操作 | 預期結果 | 結果／備註 |
|---|---|---|---|
| 7.1 | 未安裝／未登入 Claude 或 Codex 的電腦啟用開關 | 顯示 `No use` 或明確原因；程式不當掉 |  |
| 7.2 | 已登入 Claude Desktop 時啟用 Claude limits | 徽章或選單顯示 Claude 用量；無法讀取時有明確原因 |  |
| 7.3 | 已登入 Codex Desktop 時啟用 Codex limits | 徽章或選單顯示 Codex 用量；需要登入時明確提示 |  |
| 7.4 | 同時啟用 Claude 與 Codex limits | 徽章分成上下兩行：第一行 Claude、第二行 Codex |  |
| 7.5 | 任一用量超過 90%（若可測） | 對應徽章變紅警示 |  |
| 7.6 | 關閉兩個 limits | 聊天與文件助手仍可正常使用 |  |

## 8. 系統匣、排程與結束

| # | 操作 | 預期結果 | 結果／備註 |
|---|---|---|---|
| 8.1 | 在工作列右下角右鍵 ClaudeCat 圖示 | 看到顯示貓、隱藏貓、快速提問、文件助手、結束 |  |
| 8.2 | 選「隱藏貓」再選「顯示貓」 | 貓咪與用量徽章正確隱藏／顯示 |  |
| 8.3 | 右鍵 →「排程…」 | 開啟排程頁，可建立每日、每週或每小時提醒 |  |
| 8.4 | 右鍵 →「開啟記錄」 | 顯示本機記錄；記錄中不應有 API Key 或 Token |  |
| 8.5 | 系統匣或右鍵 →「結束」 | 貓咪、徽章與背景程序都結束；再次可正常啟動 |  |

---

## 問題回報格式

請每個問題附上：

1. 驗收編號（例如 `6.2`）。
2. 操作步驟與實際結果。
3. 截圖（先遮蔽文件內容、帳號、內網 URL 與憑證）。
4. `%LOCALAPPDATA%\ClaudeCat\claudecat.log` 的相關時間附近訊息（先遮蔽敏感內容）。

## 最終結論

- [ ] 可交付測試使用者
- [ ] 有阻塞問題，需修正後複驗

測試人員簽名／日期：____________________________
